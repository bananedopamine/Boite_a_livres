{# @author : Dufour Marc (marc.dufour@stjosup.com)
 # @version : 1.3
 # @lastUpdate : 22/01/2026 (Logique SPA - Orchestration des modales)
 #}

{% extends 'base.html.twig' %}

{% block body %}
<div class="container home-container">
    <h1>Gestion de Stock IES</h1>
    <p>Choisissez une action pour commencer le scan.</p>
    
    <nav class="home-nav-menu">
        {# BOUTON ENTRÉE : On passe 'false' pour indiquer que ce n'est PAS une sortie #}
        <button class="btn btn-primary btn-lg" onclick="ouvrirScan('false')">
            Déposer un livre
        </button>

        {# BOUTON SORTIE : On passe 'true' pour indiquer que c'est une sortie #}
        <button class="btn btn-warning btn-lg" onclick="ouvrirScan('true')">
            Prendre un livre
        </button>

        <a href="{{ path('app_livre_index') }}" class="btn btn-info btn-lg">
            Bibliothèque
        </a> 
    </nav>
</div>
{% endblock %}

{% block javascripts %}
<script>
    const modalePrincipale = document.getElementById('modale_principale');
    let typeActionActuel = 'false'; // Garde en mémoire si c'est Entrée ou Sortie

    /**
     * 1. Ouvre la première modale (le scan ISBN)
     */
    function ouvrirScan(action) {
        typeActionActuel = action;
        const url = "{{ path('app_mouvement_debut_fragment') }}?action=" + action;
        chargerModale(url);
    }

    /**
     * 2. ÉCOUTEUR GLOBAL DE SOUMISSION (Le Cerveau)
     * On intercepte tous les formulaires qui apparaissent dans la modale
     */
    modalePrincipale.addEventListener('submit', async (e) => {
        e.preventDefault();
        const formulaire = e.target;
        const actionUrl = formulaire.action;
        const formData = new FormData(formulaire);

        // --- CAS A : Soumission du SCAN ISBN ---
        if (actionUrl.includes('verification') || formulaire.querySelector('#isbnInput')) {
            const isbn = formulaire.querySelector('#isbnInput').value;
            verifierIsbn(isbn);
            return;
        }

        // --- CAS B : Soumission du NOUVEAU LIVRE ou CONFIRMATION ---
        try {
            const reponse = await fetch(actionUrl, {
                method: 'POST',
                body: formData,
                headers: { 'X-Requested-With': 'XMLHttpRequest' }
            });

            const resultat = await reponse.json();

            if (resultat.success) {
                if (resultat.id) {
                    // Si on vient de créer un livre, on passe à la confirmation finale
                    ouvrirConfirmation(resultat.id);
                } else {
                    // Si on a finalisé le mouvement, on ferme tout
                    modalePrincipale.close();
                    window.location.reload(); // Pour afficher le message flash de succès
                }
            }
        } catch (erreur) {
            console.error("Erreur soumission formulaire:", erreur);
        }
    });

    /**
     * 3. Logique de vérification ISBN (Appel API)
     */
    async function verifierIsbn(isbn) {
        try {
            // Avant de vérifier, on synchronise le type d'action au cas où l'utilisateur l'a changé dans le select
            const selectAction = document.getElementById('type_action_select');
            if (selectAction) {
                typeActionActuel = selectAction.value;
            }

            const reponse = await fetch(`/livre/api/verif-isbn/${isbn}`);
            const resultat = await reponse.json();

            if (resultat.statut === 'existe') {
                // Le livre est en BDD -> Go confirmation
                ouvrirConfirmation(resultat.id);
            } 
            else if (resultat.statut === 'google') {
                // Trouvé sur Google -> Go formulaire création pré-rempli
                ouvrirFormulaireNouveau(resultat.donnees);
            } 
            else {
                // Pas trouvé -> Affichage "Livre inconnu" (On le fera dans la prochaine étape)
                document.getElementById('contenu_modale').innerHTML = `
                    <h3>Livre inconnu</h3>
                    <p>L'ISBN <strong>${resultat.isbn}</strong> n'a pas été trouvé.</p>
                    <div class="modal-actions-between" class=".btn-modal">
                        <button class="btn btn-primary" onclick="ouvrirFormulaireNouveau({isbn: '${isbn}'})">
                            Créer manuellement
                        </button>
                        <button class="btn" onclick="ouvrirScan('${typeActionActuel}')">Réessayer</button>
                    </div>
                `;
            }
        } catch (erreur) {
            console.error("Erreur API ISBN:", erreur);
        }
    }

    /**
     * 4. Affiche le formulaire de création de livre
     */
    function ouvrirFormulaireNouveau(donnees) {
        const params = new URLSearchParams(donnees).toString();
        const url = "{{ path('app_livre_new') }}?" + params;
        chargerModale(url);
    }

    /**
     * 5. Affiche l'étape finale (Nom/Prénom)
     */
    function ouvrirConfirmation(livreId) {
        const url = `/mouvement/confirmation-fragment/${livreId}?type_action=${typeActionActuel}`;
        chargerModale(url);
    }
</script>
{% endblock %}