{% extends 'base.html.twig' %}

{% block body %}

<div class="container" style="text-align: center; margin-top: 50px;">
    <h1>Bienvenue dans la gestion de stock</h1>
    
    <nav style="display: flex; justify-content: center; gap: 20px; margin-top: 30px;">
        
        {# BOUTON 1 : ENTRÉE (Dépot) #}
        <a href="{{ path('app_mouvement_debut', {'action': 'false'}) }}">
            <button class="btn btn-primary btn-lg">Déposer un livre (Entrée)</button>
        </a>

        {# BOUTON 2 : SORTIE (Retrait) #}
        <a href="{{ path('app_mouvement_debut', {'action': 'true'}) }}">
            <button class="btn btn-warning btn-lg">Prendre un livre (Sortie)</button>
        </a>

        
        <a href="{{ path('app_livre_index') }}">
            <button class="btn btn-info btn-lg">Bibliothèque</button>
        </a> 

    </nav>
</div>

{% endblock %}